# Local dev dependencies for the Apple-Silicon machine (no vllm — it won't build on arm64
# and is not imported by any local code path: agent / server / eval / tests).
# Canonical reproducible env for the H100 lives in pyproject.toml + uv.lock.
# Versions mirror the ranges in pyproject.toml. Install into the project venv:
#     uv venv && uv pip install -r requirements-local.txt
langgraph>=1.0,<2.0
langchain>=1.0,<2.0
langchain-openai>=1.0,<2.0
langfuse>=4.0,<5.0
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
pydantic>=2.0,<3.0
httpx>=0.27,<1.0
python-dotenv>=1.0,<2.0
datasets>=2.20
tqdm>=4.66,<5.0
pytest>=8.0,<9.0
